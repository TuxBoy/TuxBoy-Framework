{% extends 'layout.twig' %}

{% block body %}

    <div class="container">
        <h1>Blog</h1>

        {% for article in articles %}
           <ul>
               <h1><a href="/blog/{{ article.slug }}">{{ article.name }}</a></h1>
           </ul>
        {% endfor %}
    </div>

{% endblock %}
